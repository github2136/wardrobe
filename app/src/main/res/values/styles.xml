<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
        <item name="android:actionBarSize">56dp</item>
        <item name="android:textViewStyle">@style/DefTextView</item>
        <item name="android:spinnerItemStyle">@style/SpStype</item>
    </style>

    <style name="DefTextView" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/colorTextPrimaryLight</item>
        <item name="android:textSize">@dimen/text_size_body</item>
    </style>

    <style name="EditTextMulti" parent="Base.Widget.AppCompat.EditText">
        <item name="android:background">@drawable/bg_edit_multi</item>
    </style>

    <style name="ToolBar.Theme" parent="@style/ThemeOverlay.AppCompat.Dark">
        <item name="android:tint">@android:color/white</item>
    </style>

    <style name="ToolBar.PopTheme" parent="@style/ThemeOverlay.AppCompat.Light">
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="SpStype" parent="Widget.AppCompat.TextView.SpinnerItem">
        <item name="android:textSize">18sp</item>
    </style>
</resources>
